{
  "$comment": "the registry of ReleaseTwin's own feature flags for the engine/CLI. Embedded as a resource in ReleaseTwin.Core. The hosted platform keeps its own copy of the hosted-surface flags.",
  "flags": [
    {
      "key": "flag-seam-smoke",
      "type": "boolean",
      "default": true,
      "description": "Inert end-to-end proof that the feature-flag seam is wired on every surface. Gates nothing user-visible; safe to flip.",
      "surfaces": [
        "web",
        "hosted",
        "cli"
      ],
      "owner": "platform"
    }
  ]
}
